#!/usr/bin/env bash
set -euo pipefail
mkdir -p ~/.pi
exec podman run --rm -it \
  --userns=keep-id \
  --security-opt no-new-privileges \
  --cap-drop=ALL \
  -v "$PWD:/work:z" \
  -v "$HOME/.pi:/home/node/.pi:z" \
  -w /work \
  -e TERM \
  agent-box:latest pi "$@"
